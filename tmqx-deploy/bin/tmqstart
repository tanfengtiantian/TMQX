#!/bin/sh

if [ -z "$TMQX_HOME" ] ; then
  ## resolve links - $0 may be a link to maven's home
  PRG="$0"

  # need this for relative symlinks
  while [ -h "$PRG" ] ; do
    ls=`ls -ld "$PRG"`
    link=`expr "$ls" : '.*-> \(.*\)$'`
    if expr "$link" : '/.*' > /dev/null; then
      PRG="$link"
    else
      PRG="`dirname "$PRG"`/$link"
    fi
  done

  saveddir=`pwd`

  TMQX_HOME=`dirname "$PRG"`/..

  # make it fully qualified
  TMQX_HOME=`cd "$TMQX_HOME" && pwd`

  cd "$saveddir"
fi

export JMQTT_HOME

sh ${TMQX_HOME}/bin/runbroker.sh org.tmqx.broker.BrokerStartup $@
